Install:

Global:
- npm install -g grunt-cli

Project:
- go into the project folder, open cmd and execute
- npm install 
- start server with
- grunt http-server 
- localhost open automatacally in default webbrowser
 

